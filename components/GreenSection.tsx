'use client'

import { motion } from 'framer-motion'

export default function GreenSection() {
  return (
    <section className="relative w-full bg-green-500 min-h-[30vh] mt-0">
      <div className="max-w-[1920px] mx-auto">
        {/* Green section content */}
      </div>
    </section>
  )
}
